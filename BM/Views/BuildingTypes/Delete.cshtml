<!-- Added Notes Section -->
<div>
    <h4>Notes</h4>
    <form asp-action="AddNote" method="post">
        <div class="form-group">
            <label for="Note">Add a Note</label>
            <textarea id="Note" name="Note" class="form-control" rows="3"></textarea>
        </div>
        <input type="hidden" name="BuildingTypeId" value="@Model?.Id" />
        <button type="submit" class="btn btn-info">Add Note</button>
    </form>

    <h5>Existing Notes</h5>
    <ul>
        <!-- Example notes to be replaced with actual model data -->
        <li>Double-check zoning regulations before deletion.</li>
        <li>Ensure backups have been created.</li>
    </ul>
</div>

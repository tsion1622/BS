@model BM.Models.RoomPropertyType

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete Room Property Type</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Room Property Type Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IsActive)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IsActive)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IsDeleted)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IsDeleted)
        </dd>
    </dl>
</div>

<div>
    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Delete</button> <!-- Changed input to button for better semantics -->
        <a asp-action="Index" class="btn btn-secondary">Back to List</a> <!-- Button styling for the back link -->
    </form>
</div>